[package]
name = "eidolon-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "eidolon"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
crossterm = "0.28"
eidolon-core = { path = "../eidolon-core" }
serde_json = { workspace = true }
